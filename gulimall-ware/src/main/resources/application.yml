spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.100.2/gulimall_wms?useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 11000
